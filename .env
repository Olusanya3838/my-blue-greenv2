# Blue/Green Deployment Configuration
# The CI/grader will set these values

# Container Images (provided by grader)
BLUE_IMAGE=olusanya3838/nodejs-app:blue
GREEN_IMAGE=olusanya3838/nodejs-app:green

# Which pool is active: "blue" or "green"
# This determines which service gets traffic first
ACTIVE_POOL=blue

# Release identifiers that show up in X-Release-Id header
RELEASE_ID_BLUE=v1.0.0-blue
RELEASE_ID_GREEN=v1.0.0-green

# Port the Node.js app runs on inside containers
PORT=3000
